let baseRadius = 3;
let heightCylinder = 1;

const cylinder = 2 * baseRadius * (baseRadius + heightCylinder)

console.log(`For cylinder with area ${baseRadius} and heigth ${heightCylinder}, your total area is: ${cylinder}`)